from numberConverter.numberConverter import convert_to_base_n, convert_base




# Example usage:
number_a = "1A"  # Replace with your number
number_b = "1B"

result = add_numbers(number_a, number_b, 16)
#result = convert_base(number_to_convert, from_base, to_base)
print(f"HEllo: {number_a} in base {number_b} is {result} in base 16")

